

# name: Deploy Yeng Website to Minikube

# on:
#   push:
#     branches:
#       - master

# jobs:
#   deploy:
#     runs-on: self-hosted

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Verify tools
#         shell: cmd
#         run: |
#           kubectl version --client
#           minikube version
#           docker version

#       - name: Start Minikube
#         shell: cmd
#         run: |
#           minikube start --driver=docker --cpus=4 --memory=4096

#       - name: Configure Minikube Docker daemon
#         shell: cmd
#         run: |
#           FOR /F "tokens=*" %%i IN ('minikube docker-env --shell cmd') DO %%i

#       - name: Build Docker image
#         shell: cmd
#         run: |
#           docker build -t yeng-website:latest .

#       - name: Use Minikube context
#         shell: cmd
#         run: |
#           kubectl config use-context minikube

#       - name: Deploy to Minikube
#         shell: cmd
#         run: |
#           kubectl apply -f deployment.yaml
#           kubectl apply -f services.yaml

#       - name: Verify deployment
#         shell: cmd
#         run: |
#           kubectl get pods
#           kubectl get services
#           docker images


name: Deploy Yeng Website to Minikube

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Verify tools and cluster
        shell: cmd
        run: |
          docker version
          kubectl version --client
          kubectl config current-context
          kubectl get nodes

      - name: Build Docker image (Minikube already uses Docker)
        shell: cmd
        run: |
          docker build -t yeng-website:latest .

      - name: Deploy to Minikube
        shell: cmd
        run: |
          kubectl apply -f deployment.yaml --validate=false
          kubectl apply -f services.yaml --validate=false

      - name: Verify deployment
        shell: cmd
        run: |
          kubectl get pods
          kubectl get services

